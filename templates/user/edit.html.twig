{% extends 'base.html.twig' %}

{% block title %}Modification utilisateur
{% endblock %}

{% block body %}
	<main class="container-fluid">
		<h1 class="text-primary text-center my-4 fw-bolder">Modifier l'utilisateur #
			{{user.id}}</h1>
		<div class="row align-items-center m-auto w-75">
			{{ include('user/_formEdit.html.twig', {'button_label': 'Modifier'}) }}
		</div>
		<div class="d-flex justify-content-center mt-5">
			{{ include('user/_delete_form.html.twig') }}
		</div>
	</main>
{% endblock %}
